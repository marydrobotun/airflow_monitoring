global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'severity']
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h
  receiver: 'Telegram'
  
  routes:
  - match:
      severity: critical
    receiver: Telegram
    group_by: [alertname, instance]  # только эти labels

receivers:
  - name: 'Telegram'
    telegram_configs:
      - bot_token: '7425834173:AAES7R2b1QICYD0y4jP_5XXBAY4vD-RfHoE'
        chat_id: -1003056173848
        send_resolved: true


templates:
  - '/etc/alertmanager/*.tmpl'